Some advanced user command
sudo usermod -u 2000 name
sudo groupmod -g 2000 groupname
sudo userdel -r ars
by user add create add there file is not on home directory
sudo useradd name
u use sudo mkhomedir_helper name now name hs directory in home
to change shell sudo usermod name - 
create a new group sudo groupadd <groupname>
groups name gives u the group name
sudo usermod -aG arsema name
to remove a user from a group sudo gpasswd -d name arsema

sudoers file
sudoers file is a file linux and unix adminstrators use to allocate system rights to system users
sudo visudo
